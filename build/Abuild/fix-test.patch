diff --git a/tests/client_test.c b/tests/client_test.c
index 6f14885..91eeab5 100644
--- a/tests/client_test.c
+++ b/tests/client_test.c
@@ -1057,7 +1057,9 @@ static void test_blocking_connection_errors(void) {
              strcmp(c->errstr, "Temporary failure in name resolution") == 0 ||
              strcmp(c->errstr, "hostname nor servname provided, or not known") == 0 ||
              strcmp(c->errstr, "no address associated with name") == 0 ||
-             strcmp(c->errstr, "No such host is known. ") == 0));
+             strcmp(c->errstr, "No such host is known. ") == 0 || 
+             strcmp(c->errstr, "Address family for hostname not supported") == 0 
+             ));
         valkeyFree(c);
     } else {
         printf("Skipping NXDOMAIN test. Found evil ISP!\n");
diff --git a/tests/test.sh b/tests/test.sh
index d4d3570..db50a79 100755
--- a/tests/test.sh
+++ b/tests/test.sh
@@ -130,7 +130,14 @@ fi
 echo waiting for server
 while [ ! -S "${SOCK_FILE}" ]; do
     2>&1 echo "Waiting for server..."
-    ps aux|grep valkey-server
+    case $(uname -s) in
+        QNX)
+            pidin|grep valkey-server
+            ;;
+        *)
+            ps aux|grep valkey-server
+            ;;
+    esac
     sleep 1;
 done
 
